 document.addEventListener("DOMContentLoaded", () => { const toggleBtn = document.getElementById("darkModeToggle"); const body = document.body; const darkModeEnabled = localStorage.getItem("darkMode") === "enabled"; body.classList.toggle("dark-mode", darkModeEnabled); toggleBtn.textContent = darkModeEnabled ? "Skift til Light Mode" : "Skift til Dark Mode"; toggleBtn.addEventListener("click", () => { const isDarkMode = body.classList.toggle("dark-mode"); localStorage.setItem("darkMode", isDarkMode ? "enabled" : "disabled"); toggleBtn.textContent = isDarkMode ? "Skift til Light Mode" : "Skift til Dark Mode"; }); }); 